# Terraform-VPC
Creating Virtual private Cloud using Terraform

git clone https://github.com/dhirajnimkande23/Terraform-VPC.git

cd Terraform-VPC

✅ How to Use
Step 1: Initialize Terraform
terraform init
Step 2: Validate Configuration
terraform validate
Step 3: View Execution Plan
terraform plan
Step 4: Apply to Provision Resources
terraform apply
Type yes to confirm.

🔁 To Destroy Infrastructure
terraform destroy
⚠️ Prerequisites
AWS CLI configured or valid AWS credentials exported as environment variables.
Update ami_id based on your AWS region (e.g., use latest Amazon Linux 2 AMI).
Terraform installed (>=1.0 recommended).
